#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>

/**
 * _strlen - produces the length of a string
 * s: the string whose length is being checked
 * Return: 0 on success
 */

int _strlen(char *s)
{
	return strlen(s);
}
